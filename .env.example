# Optional: production CORS-proxy origin for Notion (e.g. your Cloudflare Worker).
# Only needed for static hosts that can't rewrite requests (e.g. GitHub Pages).
# On Vercel/Netlify leave this blank — the host proxies Notion for you.
VITE_NOTION_PROXY=
